personal {!id, inserted timestamp} {
  inventory {inventory_specific_id} {
    stock {price numeric(10, 4)}
      -> physical_item

    physical_item {location, dimension}
      -> item

    item {name, description?, type {'generic', 'food', 'tool'}}
  }

  skills {
    skill {name}

    table {attribute} -> skill
  }

  some_other_schema {
    with {a, whole, bunch}

    of {stuff}
      -> but_not

    but_not {that, much}
      -> inventory.stock
  }
}